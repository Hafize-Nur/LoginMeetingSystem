@model IEnumerable<LoginMeetingSystem.Models.Meeting>
@{
    ViewData["Title"] = "Meeting";
}
<!-- Custom site CSS -->
<link href="~/css/site.css" rel="stylesheet" asp-append-version="true" />

<h2>My Meetings</h2>

<p>
    <a asp-action="Create" class="btn btn-success">Create New Meeting</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>Title</th>
            <th>Start</th>
            <th>End</th>
            <th>Description</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Title</td>
                <td>@item.StartDate.ToString("g")</td>
                <td>@item.EndDate.ToString("g")</td>
                <td>@item.Description</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-warning">Edit</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

<p>
    <a asp-controller="User" asp-action="Profile" class="btn btn-primary">
        Anasayfaya dön
    </a>
</p>
